# Telegram bot

The Telegram bot is implemented as a Supabase Edge Function: `supabase/functions/declaw-telegram-bot`.

Deploy with Supabase CLI and set `TELEGRAM_BOT_TOKEN` and `APP_BASE_URL` in the function secrets.
